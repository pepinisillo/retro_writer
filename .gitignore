# Artefactos de compilación (CMake)
/build/

# Configuración local: no se versionan para que un `git clone` deje el repo limpio
# y la app cree o regenere estos archivos al ejecutarse (ver README).
/appearance.cfg
/workspace.cfg
/visuals.cfg
/ia_writer.cfg
/ia_escrituras.jsonl
/ia_solicitudes.jsonl
/ascii_fonts/*
!/ascii_fonts/.gitkeep
